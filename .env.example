# Database Configuration
# This matches the Docker PostgreSQL container settings below
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://dev:pass@localhost:5432/farmart

# Flask Configuration
FLASK_APP=app
FLASK_ENV=development

# Security Keys - IMPORTANT: Generate your own random strings!
# Run this command to generate: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production-using-command-above
JWT_SECRET_KEY=dev-jwt-secret-change-in-production-using-command-above
JWT_ACCESS_TOKEN_EXPIRES=3600

# CORS - Frontend URL (Vite runs on 5173, not 3000)
CORS_ORIGINS=http://localhost:5173
